# AGENTS.md

## 実装方針

* 既存の C# プロジェクト構成と命名規則に合わせて実装してください。
* 仕様や挙動が不明な場合は、既存コードとユーザーの指示を優先して判断してください。
* 指示がない大規模リファクタリング、無関係な自動整形、一括整形は避けてください。

## 新しいファイルとフォルダの追加

* 作業上、新しいファイルやフォルダが必要になった場合は、勝手に配置場所を決めて作成しないでください。必要性をユーザーへ伝え、配置場所の指定を受けてから追加してください。
* テストコードや補助スクリプトなど、ユーザーが直接指定していない成果物についても同様です。
* ソリューションファイル (`.sln`) のあるディレクトリを基準とする相対パス `../Tests/` は、テストコードとその補助ファイルの配置先としてユーザーの承認済みです。この用途で同ディレクトリ以下へ追加する場合、事前確認は不要です。
* `C:/temp/` は、一時的なファイルやフォルダの配置先としてユーザーの承認済みです。この用途で同ディレクトリ以下へ追加する場合、事前確認は不要です。ただし、既存のファイルやフォルダを変更・削除しないでください。
* `C:/temp/` は定期的にクリアされるため、永続的に保持する必要がある成果物は配置しないでください。

## ソリューションとプロジェクトの呼称

* このリポジトリでは、1つのソリューションに含まれる C# プロジェクトは必ず1つです。
* 作業指示や会話で「プロジェクト」と呼ぶ場合、ソリューション全体を指していることがあります。

## ビルド

* テストビルドの方法は、この `AGENTS.md` の適用範囲配下にある `AGENTS_HowToTestBuild.md` を参照してください。

## 文字コードと改行コード

以下のルールを厳守してください。

* `.cs` ファイル
  * UTF-8 with BOM
  * CRLF

* `.md` ファイル
  * UTF-8 with BOM
  * CRLF

* `.txt` ファイル
  * Shift_JIS (CP932)
  * CRLF

* `.bat` ファイル
  * Shift_JIS (CP932)
  * CRLF

* 既存ファイルの文字コードや改行コードを、無関係な修正で変更しないでください。
* ファイルの自動変換や一括整形は禁止です。
